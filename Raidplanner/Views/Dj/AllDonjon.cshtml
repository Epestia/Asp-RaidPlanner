@model IEnumerable<Raidplanner.Models.Forms.CreateDjForm>

@{
    ViewData["Title"] = "Liste des donjons";
}

<h2>Liste des Donjons</h2>

<table class="table">
    <thead>
        <tr>

            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                
            </tr>
        }
    </tbody>
</table>
